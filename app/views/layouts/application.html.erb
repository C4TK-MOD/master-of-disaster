<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "HelpOut" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    

    <%= javascript_include_tag "application" %>
  </head>
  <body>

    <% unless content_for?(:hide_nav) %>
    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
           <a class="brand" href="#"><img src="/assets/helpoutlogosmall.png" style="height: 38px; margin-bottom: -10px; margin-top: -10px;"></a>
           <ul class="nav">
              <li><a href="#">HelpNow <%= content_for?(:title) ? yield(:title) : "" %></a></li>
           </ul>
           <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <div class="container nav-collapse">
            <ul class="nav">
<% if current_user %>
              <li><%= link_to "Home", "/home"  %></li>
              <li><%= link_to "Profile", "/profile"  %></li>
              <li><%= link_to "Events", "/events"  %></li>
              <li><%= link_to "Certifications", "/my_certifications"  %></li>
<% else %>
              <li><%= link_to "Login", "/login"  %></li>
<% end %>
<% if admin? %>
              <li><%= link_to "Admin", "/admin"  %></li>
<% end %>
<% if current_user %>
              <li><%= link_to "Logout", "/logout"  %></li>
<% end %>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>
    <% end %>

    <div class="container">
        <div class="row-fluid">
          <div class="span9">
            <%= bootstrap_flash %>
            <%= yield %>
          </div>
        </div><!--/row-->

      <!-- <footer>
        <p>&copy; Company 2013</p>
      </footer> -->

    </div> <!-- /container -->

  </body>
</html>
