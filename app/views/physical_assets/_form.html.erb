<%= form_for(@physical_asset) do |f| %>
  <% if @physical_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@physical_asset.errors.count, "error") %> prohibited this physical_asset from being saved:</h2>

      <ul>
      <% @physical_asset.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br />
    <%= f.number_field :category_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
